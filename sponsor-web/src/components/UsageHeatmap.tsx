import { BarChart3 } from "lucide-react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { useUsage } from "@/hooks/useUsage"
const DEEPL_RATE=25
function formatChars(value:number){return value>=1000000?`${(value/1000000).toFixed(2)}M`:value>=1000?`${(value/1000).toFixed(1)}K`:String(value)}
export function UsageHeatmap(){const{data,loading}=useUsage();if(loading)return <Card className="animate-fade-in"><CardHeader><CardTitle className="flex items-center gap-2"><BarChart3 className="size-5"/>用量</CardTitle></CardHeader><CardContent><div className="h-28 animate-pulse rounded-xl bg-muted"/></CardContent></Card>;if(!data)return null;const cells=data.days.map(day=>{const level=day.input_chars===0?0:day.input_chars>12000?4:day.input_chars>9000?3:day.input_chars>6000?2:1;return{...day,level}});const equivalent=data.total_input_chars/1000000*DEEPL_RATE;return <Card className="animate-fade-in"><CardHeader><CardTitle className="flex items-center gap-2"><BarChart3 className="size-5"/>用量</CardTitle></CardHeader><CardContent className="grid gap-5"><div className="grid grid-cols-2 gap-4 sm:grid-cols-4"><div><p className="text-2xl font-semibold tracking-tight">{formatChars(data.total_input_chars)}</p><p className="mt-1 text-xs text-muted-foreground">字符 / 近 12 周</p></div><div><p className="text-2xl font-semibold tracking-tight">{data.total_request_count.toLocaleString()}</p><p className="mt-1 text-xs text-muted-foreground">请求次数</p></div><div><p className="text-2xl font-semibold tracking-tight">${equivalent.toFixed(2)}</p><p className="mt-1 text-xs text-muted-foreground">DeepL 等效价</p></div><div><p className="text-2xl font-semibold tracking-tight">{data.total_error_count}</p><p className="mt-1 text-xs text-muted-foreground">错误次数</p></div></div><div className="grid gap-2"><div className="flex items-center justify-between text-xs text-muted-foreground"><span>使用热力图</span><span>少 <span className="mx-1 inline-flex gap-1 align-middle">{[0,1,2,3,4].map(level=><i key={level} className={`size-3 rounded-sm bg-heat-${level}`} />)}</span>多</span></div><div className="grid grid-flow-col grid-rows-7 gap-1 overflow-x-auto rounded-xl border bg-muted/20 p-3">{cells.map(day=><div key={day.date} title={`${day.date} · ${formatChars(day.input_chars)} 字符 · ${day.request_count} 次请求`} className={`size-3 rounded-sm bg-heat-${day.level}`} aria-label={`${day.date} ${day.input_chars} 字符`} />)}</div></div><p className="text-xs text-muted-foreground">按 DeepL API Pro 公开价格 $25 / 100 万字符估算。</p></CardContent></Card>}
